<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="witdth=device-width, initial-scale=1.0">
    <title>Guestbook</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            background-color: #e0dada;
        }
        #wrapper {
            width: 100%;
            display: flex;
            flex-direction: column;

        }
        header {
            display: flex;
            flex-direction: column;
            justify-content: space-around;
            height: 45vh;
            background-color: #4e4d4d;
            padding: 0;
            margin: 0;
            font-family: "Montserrat", sans-serif;
        }
        header p {
            align-self: center;
            font-size: 4rem;
            text-transform: uppercase;
            text-shadow: 2px 2px rgba(0,0,0,0.43);
            margin: 0;
            padding: 0;

        }

        #content {

            width: 80vw;
            background-color: #ffffff;
            -webkit-box-shadow: 3px 3px 1px 0px rgba(0,0,0,0.75);
            -moz-box-shadow: 3px 3px 1px 0px rgba(0,0,0,0.75);
            box-shadow: 3px 3px 1px 0px rgba(0,0,0,0.75);
            transform: translate(13%,-12%);
            padding: 30px;
            box-sizing: border-box;
        }

        .user-entry:nth-child(2){
            padding: 5px 2px 5px 2px;
            background-color: #eee8e8;
        }

        form {
            display: flex;
            flex-direction: column;

        }

        form p {
            align-self: center;
        }
        form input {
            align-self: center;
            width: 30vw;
            height: 10vh;
        }
    </style>
</head>
<body>
    <div id="wrapper">
        <header>
            <p>Sign the Guestbook!</p>
        </header>
        <div id="content">
            {% for comment in commentsList %}
               <div class="user-entry"><p>Nickname: {{ comment.getNickname() }} <p>Description: {{ comment.getDescription() }} </p><p>Date: {{ comment.getDate() }} </p><p>Id: {{ comment.getId() }} </p></div>
            {% endfor %}
        <form method="POST">
            <p>Name: </p>
            <input type="text" name="name">
            <p>Description: </p>
            <input type="text" name="description">
            <input type="submit" value="Submit">
        </form>
    </div>
</div>
</body>

</html>
